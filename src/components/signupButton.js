import React from "react";
import { Button } from "semantic-ui-react";

const SignupButton = () => (
  <Button size="tiny">Not a User? Click here to sign up!</Button>
);

export default SignupButton;
